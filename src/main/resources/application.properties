# Server Configuration
server.port=8080

# Disable DevTools restart to prevent ClassCastException with Redis
spring.devtools.restart.enabled=false

# Application Name
spring.application.name=Crisis Monitor

# Thymeleaf Configuration
spring.thymeleaf.cache=${THYMELEAF_CACHE:true}
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# External API Configuration
api.hungermap.base-url=https://api.hungermapdata.org
api.pdc.base-url=https://apps.pdc.org/msf/rest/services/global/pdc_active_hazards/MapServer/1

# Cache Configuration — in-memory (no Redis required)
# When REDIS_HOST is set, CacheConfig can override to use Redis.
# Without Redis, Spring uses ConcurrentHashMap — fast, survives within instance lifetime.
spring.cache.type=${CACHE_TYPE:simple}

# Redis (optional — only used when REDIS_HOST is set)
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.ssl.enabled=false
spring.data.redis.timeout=5000ms
spring.data.redis.connect-timeout=5000ms

# Static Resource Cache Control (disable for development)
spring.web.resources.cache.cachecontrol.no-cache=true
spring.web.resources.cache.cachecontrol.no-store=true
spring.web.resources.cache.cachecontrol.must-revalidate=true

# AI Analysis (Qwen)
# anthropic.api.key removed - now using DASHSCOPE_API_KEY
# anthropic.model removed - now using Qwen

# Logging
logging.level.com.crisismonitor=DEBUG
logging.level.org.springframework.web.reactive.function.client=DEBUG
